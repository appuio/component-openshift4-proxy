---
parameters:
  openshift4_proxy:
    =_metadata:
      multi_tenant: true
    namespace: openshift-config
    trustedCA: null
    httpProxy: null
    httpsProxy: "${openshift4_proxy:httpProxy}"
    noProxy: []
    spec: {}
